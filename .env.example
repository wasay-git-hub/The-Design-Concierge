# OpenAI API configuration (Required for live GPT-4o Vision and LangGraph agent chat)
OPENAI_API_KEY=your_openai_api_key_here

# SQLite Database connection URL (defaults to data/design_concierge.db in local directory)
DATABASE_URL=sqlite:///./data/design_concierge.db
